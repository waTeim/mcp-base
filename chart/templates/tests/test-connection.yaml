apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcp-base.fullname" . }}-test-connection"
  labels:
    {{- include "mcp-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcp-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
